/*
# ============================================================================
# MINIMAL TEST CONFIGURATION
# ============================================================================

# Application
spring.application.name=midi-generator-test

# H2 In-Memory Database
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# JWT (must be at least 512 bits for HS512)
app.jwt.secret=testSecretKeyForJWTTokenGenerationInTestEnvironmentOnly1234567890123456789012345678901234567890
app.jwt.expiration=3600000
app.jwt.refresh-expiration=7200000

# Email (mock - won't actually send)
spring.mail.host=localhost
spring.mail.port=25
app.email.from=test@example.com

# URLs
app.frontend-url=http://localhost:5173
app.backend-url=http://localhost:8081
app.name=MIDI Generator Test

# Token Expiry
app.token.expiry.email-verification=3600
app.token.expiry.password-reset=1800

# Logging
logging.level.root=WARN
logging.level.com.midigenerator=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN

# Disable scheduling in tests
spring.task.scheduling.enabled=false

# Node.js backend (won't be used in unit tests)
midi.generator.url=http://localhost:5001/api

# CORS
app.cors.allowed-origins=http://localhost:5173,http://localhost:8081
*/
